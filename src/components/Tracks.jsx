import { Box, useTheme, Typography } from "@mui/material";

const Tracks = ({props}) => {

    return (
        <Typography>
            Coming Soon
        </Typography>
    );
};

export default Tracks;